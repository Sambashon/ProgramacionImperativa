Entonces, invocamos el compilador:

    $ fpc -Co -Cr -Miso -gl hola.pas

Se impone analizar este comando palabra por palabra:

    fpc es el nombre del compilador Free Pascal.
    hola.pas es el nombre del archivo que contiene el programa fuente (código Pascal).
    -Co -Cr son opciones que le indican al compilador que debe generar el control de rangos y desborde de operaciones aritméticas.
    -gl hace que el compilador en caso de error en tiempo de ejecución, indique la línea del código fuente donde se produce dicho error.
    -Miso es una opción que hace que el compilador se aproxime más al Pascal estándar (el del libro).

No le tengo que dar permisos a las scripts para correrlas
Las corro de la misma forma en que correría cualquier otra script ./nombreScript

Lo que me genera duda es que el compilador genera dos archivos, un [holamundo] y [holamundo.o]

holamundo.pas
     │
     ▼
  compilación
     │
     ├── holamundo.o    ← Estado de por medio, aún no es ejecutable
     │
     ▼
  enlazado (linking)    ← Combina con otras partes de RTL de pascal y bibliotecas requeridas, así generando el ejecutable
     │
     ▼
  holamundo       ← ejecutable


  anidamiento if-then-else con if-then

  if cond1 then
   if cond2 then instruccion-1
   else
      instruccion-2


El Orden de Prioridad (Jerarquía)
1. Paréntesis ( ): Tienen la máxima prioridad. Todo lo que esté adentro se calcula primero.
2. Operadores de Negación y Signo: not, - (negativo unario) y + (positivo unario).
3. Multiplicación y División: *, /, div (división entera) y mod (resto de la división).
4. Suma y Resta: + y -.
5. Operadores de Relación / Comparación: =, <>, <, >, <=, >=, in.

Si tienen la misma jerarquía entonces se calcula de izquierda a derecha