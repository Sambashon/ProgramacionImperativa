program hexadecimal;
var 
    ea, eb, ec : char;
    salida: integer; {a 100, b 10, c 1}

begin
write('Ingrese su número positivo hexadecimal de tres digitos: ');
read(ea, eb, ec);
read(eb);
read(ec);


end.